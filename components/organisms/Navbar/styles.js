const navbar = "flex items-center justify-between flex-wrap p-6";

const navbarTitleContainer = "flex items-center flex-shrink-0 mr-6";

const navbarTitle = "font-semibold text-2xl tracking-tight tracking-wide";

const navbarItemsContainer =
  "w-full block flex-grow lg:flex lg:items-center lg:w-auto lg:fixed right-5";

const navbarItems = "text-sm lg:flex-grow";

const navbarItem =
  "block mt-4 lg:inline-block lg:mt-0 mr-4 text-lg md:hover:underline cursor-pointer";

const button =
  "transition ease-in inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-black hover:bg-white mt-4 lg:mt-0";

const styles = {
  navbar,
  navbarTitleContainer,
  navbarTitle,
  navbarItemsContainer,
  navbarItems,
  navbarItem,
  button,
};

module.exports = styles;
