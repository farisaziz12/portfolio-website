const toastButton =
  "load-hidden absolute z-10 text-center bg-white text-black font-bold tracking-wider text-lg px-10 py-1.5 rounded-2xl cursor-pointer mt-16";

const toastContainer = "w-full flex justify-center";

const formContainer = "w-screen h-screen fixed z-20";

const formCancelButton = "fixed right-0 px-8 py-4 cursor-pointer z-30";

const form = "fixed w-screen h-screen lg:p-12 pt-12 pb-2 px-2";

const styles = { toastButton, toastContainer, formContainer, formCancelButton, form };

module.exports = styles;
